pipeline {
  agent none

  stages {
    stage("Publish package") {
        agent {
            ecs {
                inheritFrom "npm"
            }
        }
        steps {
            checkout scm
            sh 'npm ci'
            sh 'npm test'
        }
    }
  }
}
